# EngagED Auth Integration

## Publishing

```bash
cd engaged-auth-integration
docker build -t engagedu/open-edx-engaged-auth-integration:{{ VERSION }} .
docker push engagedu/open-edx-engaged-auth-integration:{{ VERSION }}
```
